# Backend runtime config (DO NOT commit real secrets)

PORT=8080
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>
JWT_SECRET=<change_me>

# Twilio (optional)
ACCOUNT_SID=<change_me>
AUTH_TOKEN=<change_me>
PHONE_NUMBER=+9640000000000

# CORS
# Comma-separated list of allowed web origins.
# Example: https://admin.medicare-iq.com,https://medicare-iq.com
CORS_ORIGINS=https://admin.medicare-iq.com

# Optional: serve Admin build from backend (same origin)
# 1) Build Admin (Admin/): npm install && npm run build
# 2) Copy Admin/dist to server and point ADMIN_DIST to it (optional)
SERVE_ADMIN=false
# ADMIN_DIST=/home/medicare-iq-api/htdocs/admin.medicare-iq.com/dist
